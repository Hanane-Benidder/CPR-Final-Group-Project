// This is the header file for the Tokenizing module

#pragma once // This tells the compiler to only use this file once in the whole program

// These lines make sure we don't include this file more than one time
#ifndef _TOKENIZING_H_   // This checks if _TOKENIZING_H_ hasn't been defined already
#define _TOKENIZING_H_    // If it's not defined, we define it here

// We are including standard libraries needed for input/output and string handling
#include <stdio.h>   
#include <string.h>  

// Declaring a function named 'tokenizing'
void tokenizing(void);  // This means there's a function called tokenizing that we'll use somewhere

#endif // This ends the if-statement from the start
