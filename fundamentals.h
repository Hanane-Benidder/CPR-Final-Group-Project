// This is the header for the Fundamentals module

// These lines make sure the header is included only once in the whole program
#ifndef _FUNDAMENTALS_H_   // This checks if _FUNDAMENTALS_H_ hasn't been used before
#define _FUNDAMENTALS_H_    // If it hasn't, we define it, so it's like saying "okay, now I'm using it"

// These are standard libraries we're using in our program
#include <stdio.h>   
#include <stdlib.h>  
#include <string.h>  

// This line tells our program that there's a function called 'fundamentals' we will use
void fundamentals(void);  // It doesn't have the code for the function, just tells us it exists

#endif // This ends the if-statement we started at the beginning
