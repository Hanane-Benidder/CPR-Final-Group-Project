// This is the header file for the Manipulating module

#pragma once // This tells the compiler to only include this file once in the whole program

// These lines are for making sure we don't include this file more than once
#ifndef _MANIPULATING_H_   
#define _MANIPULATING_H_    
// We are including standard libraries for input/output and string handling
#include <stdio.h>   
#include <string.h>  

// This line declares a function named 'manipulating'
void manipulating(void);  // It tells the program that we have a function called manipulating, but doesn't say what it does

#endif // This ends the if-statement that started with #ifndef
